name: SA-16 L13 Live Validation

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: sa16-l13-live-${{ github.event.pull_request.head.sha }}
  cancel-in-progress: true

jobs:
  live-governed-browser-actions:
    if: github.event.pull_request.head.ref == 'agent/sa16-l13-governed-public-browser-actions'
    runs-on: ubuntu-22.04
    timeout-minutes: 30
    steps:
      - name: Check out exact pull-request head
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          ref: ${{ github.event.pull_request.head.sha }}

      - name: Set up Python
        uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38 # v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install project and isolated browser bindings
        run: |
          python -m pip install .
          python -m pip install playwright==1.61.0

      - name: Verify dependency consistency
        run: python -m pip check

      - name: Install bounded Chromium runtime
        run: python -m playwright install --with-deps chromium

      - name: Configure controlled first-party fixture hostname
        run: echo '127.0.0.1 sa16-l13-fixture.example' | sudo tee -a /etc/hosts

      - name: Controlled typed actions, evidence and resumability validation
        run: python scripts/sa16_l13_browser_actions_live.py
