import type { ReactNode } from "react";

import "../../features/professional-context/professional-context.css";

export default function ProfessionalContextLayout({ children }: { children: ReactNode }) {
  return children;
}
